Current version is outdated

update all

import keras. to import tensorflow.keras.

this will also require a shift from keras-rl to keras-rl2
which can be achieved by 
pip(3) uninstall keras-rl --- if keras-rl is already installed
pip(3) install keras-rl2 
